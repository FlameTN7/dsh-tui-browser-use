# Build output (generated by tsc; never committed)
lib/

# Dependencies
node_modules/

# Local npm cache (this repo uses --cache .npm-cache in restricted sandboxes)
.npm-cache/

# Local install / editor
*.log
.DS_Store

# Internal agent / working-process docs (local only, never committed)
AGENTS.md
docs/
